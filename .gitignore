node_modules/
vault/
*.sqlite
*.sqlite-wal
*.sqlite-shm
npm-debug.log*

# Local AI: engine scratch (decrypted user models) — never commit
engine/cache/
# GGUF model files are large + user-supplied; keep the models/ folder, not its weights
models/*.gguf
models/*.guff

# secrets — never commit (these also live under vault/, listed again for safety)
vault/keys/
master.key
*.key
.env
